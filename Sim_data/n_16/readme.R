## values used for n_16 data

n_leaves <- 16
n_particles_vals <- seq(from = 256, to = 4096, by = 256)
n_reps <- 100
delta <- 0.1
sigma <- 0.1

# immortal line generated as RTS smoother MAP + 0,1,2,3*SD
# same observations used for all
